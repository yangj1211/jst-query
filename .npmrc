# 将 react 和 react-dom 提升到根 node_modules，避免多实例导致 useContext 为 null
public-hoist-pattern[]=*react*
public-hoist-pattern[]=*react-dom*

# 解决 CRA + pnpm 下 ESLint 插件重复实例导致的
# "Plugin \"react\" was conflicted between \"eslint-config-react-app\"" 报错
# 把 eslint 相关包也统一提升到根 node_modules
public-hoist-pattern[]=*eslint*

# 若仍有问题可改为 shamefully-hoist=true（与 npm 行为一致）
